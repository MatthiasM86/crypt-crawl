extends Node
## Global game/run-state singleton (registered as an autoload in project.godot).
##
## Scaffold only — no HP, death, or restart logic yet. See docs/plan.md,
## Phase 4, for this singleton's intended responsibilities (owns player HP,
## handles death, resets state for a new run).
